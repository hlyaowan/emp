<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quartz.monitor.dao.UaInfoDAO">
	<select id="UagentList" parameterType="Uagent" resultType="java.util.HashMap">
		SELECT `id` as id, `uainfo` as uainfo, `ip` as ip, `extend` as extend, `createTime` as createTime  FROM `ua_info`
	</select>
	
	<select id="UagentOne" parameterType="Uagent" resultType="Uagent">
		SELECT `id` as id, `uainfo` as uainfo, `ip` as ip, `extend` as extend, `createTime` as createTime  FROM `ua_info`
		<trim prefix="WHERE" prefixOverrides="AND | OR">
			<if test="id != null">
				id = #{id}
			</if>
		</trim>
	</select>

</mapper>

