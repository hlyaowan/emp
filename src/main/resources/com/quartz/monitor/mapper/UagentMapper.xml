<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quartz.monitor.dao.UaInfoDAO">
	<select id="UagentList" parameterType="Uagent" resultType="java.util.HashMap">
		SELECT  `id` as id, `mothodName` as mothodName, `number` as number, `modifyTime` as modifyTime FROM `visit_user`
	</select>
	
	<select id="UagentOne" parameterType="Uagent" resultType="Uagent">
		SELECT  `id` as id, `mothodName` as mothodName, `number` as number, `modifyTime` as modifyTime FROM `visit_user`  ORDER BY RAND() LIMIT 1
	</select>

</mapper>

