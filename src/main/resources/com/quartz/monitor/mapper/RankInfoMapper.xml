<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quartz.monitor.dao.RankInfoDAO">
	<select id="RankInfoList" parameterType="RankInfo" resultType="java.util.HashMap">
		SELECT `id` as id , `methodName` as methodName, `contentType` as contentType, `rankType` as rankType, `rankTime` as rankTime, `start` as start, `count` as count, `catalogId` as catalogId, `channelType` as channelType, `createTime` as createTime, `modifyTime` as modifyTime FROM `rank_info`
	</select>
</mapper>